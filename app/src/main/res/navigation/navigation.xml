<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/mapsActivity">
    <activity
        android:id="@+id/mapsActivity"
        android:name="com.example.project.MapsActivity"
        android:label="activity_maps"
        tools:layout="@layout/activity_maps" />
    <fragment
        android:id="@+id/textViewFrag"
        android:name="com.example.project.testViewFragment"
        android:label="fragment_test_view"
        tools:layout="@layout/fragment_test_view" >
        <action
            android:id="@+id/action_textViewFrag_to_mapsActivity"
            app:destination="@id/mapsActivity" />
    </fragment>
</navigation>